name: Coverage

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

jobs:
  coverage:
    uses: NicTool/.github/.github/workflows/coverage.yml@main
    secrets: inherit
    permissions:
      contents: read
